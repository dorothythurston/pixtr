<% if current_user.liked? @image%>
    <%= button_to "Unlike this image", unlike_image_path(@image),remote:true, method: :delete %>
<%else%>
    <%= button_to "Like this image!", like_image_path(@image), remote:true %>
    <%end%>
