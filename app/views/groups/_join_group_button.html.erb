<% if current_user.member?  @group %>
  You are a member of this group!
  <%= button_to "Leave Group", leave_group_path(@group), remote: true, method: :delete %>
  <%else%>
    <%= button_to "Join Group", join_group_path(@group), remote: true %>
  <%end%>
